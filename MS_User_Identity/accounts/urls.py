# MS_User_Identity/accounts/urls.py

from django.urls import path

# Đây là file routing của ứng dụng 'accounts'.
# Hiện tại chưa có View nào, nên chúng ta để trống.
urlpatterns = [
    # path('users/', UserListView.as_view(), name='user-list'), # Sẽ thêm sau
]